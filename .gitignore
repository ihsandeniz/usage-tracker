__pycache__/
*.pyc
*.log
.venv/

# runtime / calibration
usage_calib.json
view_config.json

# generated by install.sh (machine-specific)
surface/surface.conf

# vault-only working notes — MUST NOT reach the public repo
CLAUDE.md

# PyInstaller yerel derlemesi (sürüm ikilileri CI'dan gelir — bkz. packaging/usage-tracker.spec)
build/
dist/
*.spec.bak
